#pragma once
#include "typedefs.hpp"
#include <time.h>
#include <string>

namespace Model
{
    namespace Game
    {
        extern scoreInt player_score;
        void reset();
        void addPoint();
    }
}
